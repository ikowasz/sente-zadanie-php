{% if attribute(order, col).timestamp is defined %}
    {{ attribute(order, col)|date }}
{% elseif col == 'invoiced' %}
    {{ (order.invoiced ? 'yes' : 'no')|trans }}
{% else %}
    {{ attribute(order, col) }}
{% endif %}
